import { Request, Response } from "express";

export const handleCode = async (_req: Request, res: Response) => {
  res.status(200).json({});
};

export const handleUser = async (_req: Request, res: Response) => {
  res.status(200).json({});
};
