import { Request, Response } from "express";

export const handleTopGG = async (_req: Request, res: Response) => {
  res.status(200).json({});
};

export const handleVCode = async (_req: Request, res: Response) => {
  res.status(200).json({});
};

export const handleWumpus = async (_req: Request, res: Response) => {
  res.status(200).json({});
};

export const handleDLIst = async (_req: Request, res: Response) => {
  res.status(200).json({});
};
